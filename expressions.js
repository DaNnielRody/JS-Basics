// Arithmetic expression

let sum = 4 + 2; // Dentro da atribuição da variável

// Variable expressions

let result = sum * 2; // Utilizando o valor de outras variáveis.

// Function call expressions

function square(number){
    return number**2;
}

let squaredValue = square(3) // Utilizando o retorno da função.

// Conditional expressions

let age = 21;
let isMinor = age <= 18 ? "Yes" : "No"; // Faz uso dos ternários.
